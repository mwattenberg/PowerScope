<UserControl x:Class="PowerScope.View.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerScope.View.UserControls"
             mc:Ignorable="d" 
             d:Height="26" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- MenuBar dark theme styles using global resources -->
        <Style TargetType="Menu" BasedOn="{StaticResource SystemFontStyle}">
            <Setter Property="Background" Value="{DynamicResource PlotSettings_TitleBarBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PlotSettings_LabelForegroundBrush}"/>
        </Style>

        <Style TargetType="MenuItem" BasedOn="{StaticResource SystemFontStyle}">
            <Setter Property="Background" Value="{DynamicResource PlotSettings_TitleBarBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PlotSettings_LabelForegroundBrush}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Popup Name="SubMenuPopup" IsOpen="{TemplateBinding IsSubmenuOpen}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                                <Border Background="{DynamicResource PlotSettings_TitleBarBrush}" BorderBrush="{DynamicResource PlotSettings_BorderBrush}" BorderThickness="1">
                                    <ItemsPresenter/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PlotSettings_ButtonHoverBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PlotSettings_ButtonPressedBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF888888"/>
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="SubMenuPopup" Property="IsOpen" Value="True"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource PlotSettings_BackgroundBrush}"
            BorderBrush="{DynamicResource PlotSettings_BorderBrush}"
            BorderThickness="1"
            CornerRadius="0"
            Panel.ZIndex="1000">
        <Menu Height="26" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <MenuItem Header="File">
                <MenuItem Header="Load Settings                    Ctrl+O" Click="MenuItem_LoadSettings_Click"/>
                <MenuItem Header="Save Settings                     Ctrl+S" Click="MenuItem_SaveSettings_Click"/>
                <Separator/>
                <MenuItem Header="Export Waveform             Ctrl+E" Click="MenuItem_ExportPlot_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="MenuItem_Exit_Click"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Preferences                        Ctrl+P" Click="MenuItem_Preferences_Click"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Click="MenuItem_About_Click"/>
            </MenuItem>
        </Menu>
    </Border>
</UserControl>

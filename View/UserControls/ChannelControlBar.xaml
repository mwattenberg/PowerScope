<UserControl x:Class="PowerScope.View.UserControls.ChannelControlBar"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="clr-namespace:PowerScope.View.UserControls">
    <UserControl.Resources>
        <!-- Secondary button style for Add Virtual Channel button -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
         <Setter Property="Foreground" Value="White"/>
    <Setter Property="FontWeight" Value="Normal"/>
     <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Background" Value="{StaticResource PlotSettings_TitleBarBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PlotSettings_BorderBrush}"/>
 <Setter Property="Template">
     <Setter.Value>
    <ControlTemplate TargetType="Button">
     <Border Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
     BorderThickness="{TemplateBinding BorderThickness}"
          CornerRadius="4">
  <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
         </Border>
      <ControlTemplate.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
    <Setter Property="Background" Value="{DynamicResource PlotSettings_ButtonHoverBrush}"/>
  </Trigger>
      <Trigger Property="IsPressed" Value="True">
         <Setter Property="Background" Value="{DynamicResource PlotSettings_ButtonPressedBrush}"/>
            </Trigger>
         </ControlTemplate.Triggers>
   </ControlTemplate>
       </Setter.Value>
      </Setter>
        </Style>
    </UserControl.Resources>
    <Border Height="auto" Background="{DynamicResource PlotSettings_BackgroundBrush}" CornerRadius="5" Margin="2">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
          <ItemsControl x:Name="ChannelItemsControl" Height="auto" VerticalAlignment="Stretch">
       <ItemsControl.ItemsPanel>
     <ItemsPanelTemplate>
          <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0"/>
   </ItemsPanelTemplate>
       </ItemsControl.ItemsPanel>
       <!-- DataTemplate: Automatically creates ChannelControl for each ChannelSettings -->
              <ItemsControl.ItemTemplate>
 <DataTemplate>
  <local:ChannelControl Margin="2,0,2,0"/>
    </DataTemplate>
</ItemsControl.ItemTemplate>
        </ItemsControl>

             <!-- Add Virtual Channel Button -->
<Button x:Name="AddVirtualChannelButton" 
      Click="AddVirtualChannelButton_Click"
                 Margin="2,0,2,0"
    MinWidth="110"
MinHeight="140"
  VerticalAlignment="Top"
             Style="{StaticResource SecondaryButtonStyle}"
            FontFamily="{DynamicResource {x:Static SystemFonts.MessageFontFamily}}"
          FontSize="{DynamicResource {x:Static SystemFonts.MessageFontSize}}">
      <TextBlock TextAlignment="Center" VerticalAlignment="Center">
     <Run Text="Add"/>
            <LineBreak/>
     <Run Text="Virtual"/>
          <LineBreak/>
       <Run Text="Channel"/>
                </TextBlock>
          </Button>
         </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>

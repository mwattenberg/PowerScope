<UserControl x:Class="SerialPlotDN_WPF.View.UserControls.ChannelControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SerialPlotDN_WPF.View.UserControls">
    <Border Height="auto" Background="{DynamicResource PlotSettings_BackgroundBrush}" CornerRadius="5"
            BorderBrush="{DynamicResource PlotSettings_BorderBrush}" BorderThickness="2" Margin="2">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="ChannelItemsControl" Height="190" VerticalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <!-- DataTemplate: Automatically creates ChannelControl for each ChannelSettings -->
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:ChannelControl 
                            MeasurementCommand="{Binding MeasurementCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Border>
</UserControl>

<UserControl x:Class="SerialPlotDN_WPF.View.UserControls.PlotAndMeasurementBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SerialPlotDN_WPF.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <UserControl.Resources>
        <Thickness x:Key="myMargin" Top="1" Left="1" Right="1" Bottom="1"/>
    </UserControl.Resources>
    <Grid Background="DarkGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Add new" Panel.ZIndex="1" BorderThickness="2" BorderBrush="DarkGray"/>
        <Grid Grid.Row="0" Background="DarkGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Cursor" Grid.Row="0" Grid.Column="0" Margin="7,15,1,1"/>
            <Button Content="Note" Grid.Row="0" Grid.Column="1" Margin="1,15,7,1"/>
            <Button Content="Measure" Grid.Row="1" Grid.Column="0" Margin="7,1,1,7"/>
            <Button Content="Plot" Grid.Row="1" Grid.Column="1" Margin="1,1,7,7"/>
        </Grid>
        <ListView x:Name="ListView_Measurements" Grid.Row="1" Background="DarkGray">
            <local:MeasurementBox Width="190" Margin="0,0,0,0"/>
        </ListView>
        
        <!--<local:MeasurementBox Grid.Row="1"/>
        <local:MeasurementBox Grid.Row="2"/>
        <local:MeasurementBox Grid.Row="3"/>
        <local:MeasurementBox Grid.Row="4"/>
        <local:MeasurementBox Grid.Row="5"/>
        <local:MeasurementBox Grid.Row="6"/>-->
    </Grid>
    
</UserControl>

    
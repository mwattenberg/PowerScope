<UserControl x:Class="SerialPlotDN_WPF.View.UserControls.AquisitionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SerialPlotDN_WPF.View.UserControls"
             mc:Ignorable="d" 
             Width="280" Height="150">
    <UserControl.Resources>
        <!-- Styles copied from HorizontalControl -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PlotSettings_TextBoxBackgroundBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PlotSettings_BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="AquisitionLabelTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="InfoTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{DynamicResource PlotSettings_TextBoxBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PlotSettings_BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0,5,0"/>
        </Style>
    </UserControl.Resources>
    <Border Height="Auto" Background="{DynamicResource PlotSettings_BackgroundBrush}" CornerRadius="5"
            BorderBrush="{DynamicResource PlotSettings_BorderBrush}" BorderThickness="1">
        <Grid x:Name="RootGrid" Background="{DynamicResource PlotSettings_BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>  <!-- Top color bar -->
                <RowDefinition Height="*"/>  <!-- Bits / second -->
                <RowDefinition Height="*"/>  <!-- Samples / second -->
                <RowDefinition Height="*"/>  <!-- Total Memory Size -->
                <RowDefinition Height="*"/>  <!-- CPU Load -->
            </Grid.RowDefinitions>
            <!-- Top color bar with "Aquisition" label -->
            <Border x:Name="TopColorBar" Grid.Row="0" Background="Gray" CornerRadius="2" Height="20" Margin="2">
                <TextBox x:Name="AquisitionLabelTextBox" Text="Aquisition" Style="{StaticResource AquisitionLabelTextBoxStyle}"/>
            </Border>
            <!-- Bits / second row -->
            <Grid Grid.Row="1" Margin="5,2,5,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Port usage (bps):" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Grid.Column="1" x:Name="BitsPerSecondTextBox" Style="{StaticResource InfoTextBoxStyle}" Text="0" Width="120" HorizontalAlignment="Right" Margin="5,0,0,0"/>
            </Grid>
            <!-- Samples / second row -->
            <Grid Grid.Row="2" Margin="5,2,5,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Samples / second:" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Grid.Column="1" x:Name="SamplesPerSecondTextBox" Style="{StaticResource InfoTextBoxStyle}" Text="0" Width="120" HorizontalAlignment="Right" Margin="5,0,0,0"/>
            </Grid>
            <!-- Total Memory Size row -->
            <Grid Grid.Row="3" Margin="5,2,5,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Total Memory Size / MB:" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Grid.Column="1" x:Name="TotalMemorySizeTextBox" Style="{StaticResource InfoTextBoxStyle}" Text="0" Width="120" HorizontalAlignment="Right" Margin="5,0,0,0"/>
            </Grid>
            <!-- CPU Load row -->
            <Grid Grid.Row="4" Margin="5,2,5,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="CPU Load / %:" Style="{StaticResource LabelTextStyle}"/>
                <TextBox Grid.Column="1" x:Name="CPULoadTextBox" Style="{StaticResource InfoTextBoxStyle}" Text="0" Width="120" HorizontalAlignment="Right" Margin="5,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>

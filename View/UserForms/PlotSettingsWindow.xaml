<Window x:Class="SerialPlotDN_WPF.View.UserForms.PlotSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SerialPlotDN_WPF.View.UserForms"
        mc:Ignorable="d"
        Title="Plot Settings" Height="350" Width="400" 
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen"
        Background="Transparent">
    
    <Window.Resources>
        <!-- Dark theme styles -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#FFA0ACB5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFA0ACB5"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFA0ACB5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFA0ACB5"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3E3E40"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4E4E50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Custom close button style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFFF4444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFCC3333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- Main container with border and drop shadow -->
    <Border Background="#FF1E1E1E" 
            BorderBrush="#FF3F3F46" 
            BorderThickness="1"
            CornerRadius="5">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Custom title bar -->
            <Grid Grid.Row="0" Background="#FF2D2D30" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Plot Settings" 
                           Foreground="#FFA0ACB5" 
                           FontSize="14" FontWeight="SemiBold"
                           VerticalAlignment="Center" 
                           Margin="15,8"/>
                
                <!-- Close button -->
                <Button Grid.Column="1" 
                        Style="{StaticResource CloseButtonStyle}"
                        Content="✕" 
                        Click="CloseButton_Click"
                        ToolTip="Close"/>
            </Grid>
            
            <!-- Header -->
            <TextBlock Grid.Row="1" Text="Configure plot and rendering settings" 
                       FontSize="12" 
                       Foreground="#FF888888" 
                       Margin="20,15,20,5"/>
            
            <!-- Settings Content -->
            <StackPanel Grid.Row="2" Margin="20,10,20,0">
                <!-- Plot Update Rate (FPS) -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    
                    <Label Grid.Column="0" Content="Plot Update Rate (FPS):" VerticalAlignment="Center"/>
                    <TextBox x:Name="TextBoxPlotFPS" Grid.Column="1" VerticalAlignment="Center" 
                             Text="{Binding PlotUpdateRateFPS, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <StackPanel Grid.Column="2" Orientation="Vertical" Margin="5,0,0,0">
                        <Button x:Name="ButtonPlotFPSUp" Content="▲" Height="15" FontSize="8" 
                                Click="ButtonPlotFPSUp_Click" Margin="0,0,0,1"/>
                        <Button x:Name="ButtonPlotFPSDown" Content="▼" Height="15" FontSize="8" 
                                Click="ButtonPlotFPSDown_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- Serial Port Update Rate (Hz) -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    
                    <Label Grid.Column="0" Content="Serial Update Rate (Hz):" VerticalAlignment="Center"/>
                    <TextBox x:Name="TextBoxSerialHz" Grid.Column="1" VerticalAlignment="Center" 
                             Text="{Binding SerialPortUpdateRateHz, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <StackPanel Grid.Column="2" Orientation="Vertical" Margin="5,0,0,0">
                        <Button x:Name="ButtonSerialHzUp" Content="▲" Height="15" FontSize="8" 
                                Click="ButtonSerialHzUp_Click" Margin="0,0,0,1"/>
                        <Button x:Name="ButtonSerialHzDown" Content="▼" Height="15" FontSize="8" 
                                Click="ButtonSerialHzDown_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- Line Width -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    
                    <Label Grid.Column="0" Content="Line Width:" VerticalAlignment="Center"/>
                    <TextBox x:Name="TextBoxLineWidth" Grid.Column="1" VerticalAlignment="Center" 
                             Text="{Binding LineWidth, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <StackPanel Grid.Column="2" Orientation="Vertical" Margin="5,0,0,0">
                        <Button x:Name="ButtonLineWidthUp" Content="▲" Height="15" FontSize="8" 
                                Click="ButtonLineWidthUp_Click" Margin="0,0,0,1"/>
                        <Button x:Name="ButtonLineWidthDown" Content="▼" Height="15" FontSize="8" 
                                Click="ButtonLineWidthDown_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- Anti-Aliasing -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Label Grid.Column="0" Content="Anti-Aliasing:" VerticalAlignment="Center"/>
                    <CheckBox x:Name="CheckBoxAntiAliasing" Grid.Column="1" VerticalAlignment="Center" 
                              IsChecked="{Binding AntiAliasing, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </Grid>
            </StackPanel>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,20,20,20">
                <Button x:Name="ButtonApply" Content="Apply" Width="75" Margin="0,0,10,0" Click="ButtonApply_Click"/>
                <Button x:Name="ButtonClose" Content="Close" Width="75" Click="ButtonClose_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
